- name: Deploy
  uses: tungly/midcitysecuritydisrtict.org
  with:
    service_key: {{secrets.service_key}}
    project: mid-city-security-district
    home_page_path: /index.html
    build_folder: ${{ github.workspace }}
